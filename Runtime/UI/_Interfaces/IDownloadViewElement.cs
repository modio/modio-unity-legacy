namespace ModIO.UI
{
    /// <summary>Allows a MonoBehaviour to be assigned to a DownloadView.</summary>
    public interface IDownloadViewElement
    {
        void SetDownloadView(DownloadView view);
    }
}

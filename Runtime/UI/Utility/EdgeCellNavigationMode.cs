namespace ModIO.UI
{
    /// <summary>Describes how navigation from the final element in
    /// UIUtilities.SetExplicitGridNavigation should work.</summary>
    public enum EdgeCellNavigationMode
    {
        DontWrap,
        Wrap,
        WrapAndIncrement,
    }
}

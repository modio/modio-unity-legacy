namespace ModIO.UI
{
    /// <summary>Allows a MonoBehaviour to be assigned to a ModfileView.</summary>
    public interface IModfileViewElement
    {
        void SetModfileView(ModfileView view);
    }
}

namespace ModIO.UI
{
    /// <summary>Allows a MonoBehaviour to be assigned to a SubscriptionsView.</summary>
    public interface ISubscriptionsViewElement
    {
        void SetSubscriptionsView(SubscriptionsView view);
    }
}
